# SPEC-NNNN: <short title>

**Status**: Draft | In Progress | Implemented
**Date**: YYYY-MM-DD

## What

Plain-language description of the feature or change. Focus on what it does and why it's needed —
no tech stack, no file paths, no code structure here.

## Why

The concrete reason this is needed now (a request, a gap found in an audit, a real user-facing problem).

## Scope boundaries

- In scope: ...
- Out of scope (this pass): ...

## Evidence to reuse

Existing pages, components, or lib functions this should link to or build on, rather than duplicate.

## Open questions / [NEEDS CLARIFICATION]

Anything ambiguous that should be confirmed before or during implementation, rather than guessed at.

## Verification

Exact commands that will prove the change works:
- `npx tsc --noEmit`
- `npx eslint <paths>`
- `npx vitest run <test paths>`
- `npm run build` (isolated copy if `.next` is permission-locked)
